{erl_opts, [debug_info]}.
{deps, []}.

{pre_hooks,
  [{"(linux|darwin)", compile, "make"},
   {"(freebsd)", compile, "gmake"}]}.

{post_hooks,
  [{"(linux|darwin)", clean, "make clean"},
   {"(freebsd)", clean, "gmake clean"}]}.
